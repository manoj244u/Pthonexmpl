def if_else():
	a=int(input('enter the first Number :'))
	b=int(input('Enter the second Number :'))
	c=int(input('Enter the third Number :'))
	if(a>b and a>c):
		print'the bigest Number a is:',a
	elif(b>c):
		print'B is biggest number',b
	else:
		print'C is biggest number',c
if_else();
